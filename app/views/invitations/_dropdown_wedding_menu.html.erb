<div class="separator matching-items">
    <h4 class="floatL">Wedding Menu</h4>
    <div class="floatR dropdown">
        <%= collection_select @invitation.to_name('wedding-menus'),
                                :invitation_pricing_id,
                                InvitationPricing.price_dropdown_by_cat('Wedding Menu'),
                                :id, :quantity_and_price,
                                {prompt: "None"},
                                { data:
                                    {
                                        get_url: "#{invitation_pricings_path}",
                                        price: 0
                                    },
                                    class: 'price'
                                } %>
    </div>
</div>