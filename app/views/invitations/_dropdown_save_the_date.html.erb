<div class="separator matching-items">
    <h4 class="floatL">Save The Dates</h4>
    <div class="floatR dropdown">
        <%= collection_select @invitation.to_name('save-the-dates'),
                                :invitation_pricing_id,
                                InvitationPricing.price_dropdown_by_cat('Save The Date Postcard'),
                                :id, :quantity_and_price,
                                {prompt: "None"},
                                { data:
                                    {
                                        get_url: "#{invitation_pricings_path}",
                                        price: 0
                                    },
                                    class: 'price'
                                } %>
    </div>
</div>